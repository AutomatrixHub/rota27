# Validação v0.25.95 — Modo Teste → Dados Reais

- [x] JavaScript `v02595-test-real-boundary.js` validado com `node --check`.
- [x] Asset incluído no roadmap e no App Shell.
- [x] Branch 0 commits atrás de `main` antes do PR.
- [x] Auditoria Supabase: 0 eventos `test_*` / `testMode:true` em `rota27_sync_events`.
- [ ] Validar em aparelho real: Ativar Modo Teste → Voltar aos dados reais → uma única recarga → somente comandas reais.
